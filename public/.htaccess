# public/.htaccess
# Redirige toutes les requêtes vers index.php (Front Controller)

Options -Indexes
RewriteEngine On

# Fichiers statiques servis directement
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Tout le reste → index.php
RewriteRule ^ index.php [QSA,L]
